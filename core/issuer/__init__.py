"""Issuer Registry: a synthetic/demo stand-in for an authorized issuer
connector (see core/issuer/registry.py). Import from the specific
submodule (core.issuer.registry, core.issuer.compare, ...), not this
package root -- matches the rest of core/ (e.g. core.crypto.manifest).
"""
